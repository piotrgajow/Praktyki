#ifndef SAVETOFILE_H
#define SAVETOFILE_H
#include <fstream>
#include <string>
#include <iostream>
class SaveTofile
{
public:
    SaveTofile();
    SaveTofile(std::string,std::string);

};

#endif // SAVETOFILE_H
