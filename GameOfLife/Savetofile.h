#ifndef SAVETOFILE_H
#define SAVETOFILE_H
#include <fstream>
#include <string>
#include <iostream>
#include <stdio.h>
class SaveTofile
{
public:
    SaveTofile();
    SaveTofile(std::string,std::string);

};

#endif // SAVETOFILE_H
